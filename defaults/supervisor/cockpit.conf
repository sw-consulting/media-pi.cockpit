# defaults/supervisor/cockpit.conf
[program:cockpit]
command=/bin/sh -lc '/usr/lib/cockpit/cockpit-ws --no-tls --address "${COCKPIT_ADDRESS:-0.0.0.0}" --port "${COCKPIT_PORT:-9090}"'
autostart=true
autorestart=true
stdout_logfile=/dev/fd/1
stderr_logfile=/dev/fd/2
